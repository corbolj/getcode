package getcode

import "fmt"

func Clone(url string) {
	fmt.Println("Still need to implement.")
}

func cloneRepos(url string) {
	fmt.Println("work in progress")

	// Get Organization (url) return org

	// Get list of repos (org) return repos[]

	// Clone all repos[]
}
